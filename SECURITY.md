# 安全说明

本项目只做一件事：把图片发给视觉模型 API 换回文字描述。涉及安全的主要是 **API Key 的保管**。

## API Key 使用规范

- 把 Key 放在同目录 `.env` 文件里（已被 `.gitignore` 忽略，不会提交进仓库），而不是直接写进 `vision.js`。
- **切勿**把填了真实 Key 的 `vision.js` 提交到 GitHub 或任何公开仓库。
- 如果误提交，请立即到百炼控制台**吊销并重新生成**该 Key，而不是只删掉提交记录（Key 一旦泄露即视为公开）。
- GitHub 的密钥扫描（secret scanning）会拦截部分泄露，但不要依赖它。

## 关于 `git update-index --skip-worktree` 的警告

README 曾提到用 `git update-index --skip-worktree vision.js` 防止误传 Key。请知悉它的副作用：

- 它只影响**你自己本地**的 git 索引，不会同步给其他协作者；
- 之后的仓库更新（如上游改版）在你的本地会被静默跳过，容易造成"代码没更新"的困惑；
- 取消时容易忘，建议优先使用 `.env` 方案，而不是依赖这个命令。

## 其他

- `vision.js` 只请求图片内容相关的 API，不收集、不上传任何额外数据；
- 网络图片通过 URL 直接传给服务商，传图即代表你同意将图片内容发送给该服务商处理，请注意图片本身的隐私（截图、证件照、聊天记录等敏感图慎用第三方模型）。
